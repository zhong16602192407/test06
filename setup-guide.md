# 🚀 快速配置指南

## 第 1 步：注册 Helius

1. 访问：https://www.helius.dev/
2. 点击 "Sign Up" 或 "Start Building for Free"
3. 使用 GitHub、Google 或邮箱注册
4. 验证邮箱（如果需要）

## 第 2 步：获取 API Key

1. 登录后，进入 **Dashboard**
2. 点击 "Create New Key" 或 "New Project"
3. 选择网络：**Mainnet** ✅
4. 复制你的 API Key（类似：`f8a1b2c3-4d5e-6f7g-8h9i-0j1k2l3m4n5o`）

## 第 3 步：配置项目

### 方法 1：编辑 .env 文件（推荐）

已经为你创建了 `.env` 文件，打开编辑：

```bash
nano .env
```

将这一行：
```
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY
```

替换为（把 `YOUR_HELIUS_API_KEY` 改成你的实际 Key）：
```
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=f8a1b2c3-4d5e-6f7g-8h9i-0j1k2l3m4n5o
```

保存并退出（Ctrl+O, Enter, Ctrl+X）

### 方法 2：直接设置环境变量

```bash
export SOLANA_RPC_URL="https://mainnet.helius-rpc.com/?api-key=你的API_KEY"
```

## 第 4 步：测试配置

运行测试命令：

```bash
npm install
npm run test-config
```

应该看到：
```
✅ 配置正确
✅ 可以连接到 Solana 网络
✅ 目标地址有效
```

## 第 5 步：开始使用

### 采集历史数据：
```bash
npm start
```

### 实时监控：
```bash
npm run monitor
```

### 查看结果：
```bash
ls -lh output/
```

---

## 🔍 常见问题

### Q: 如何查看我的 Helius 使用额度？

A: 登录 Helius Dashboard，可以看到当前月份的 credits 使用情况。

### Q: 100,000 credits 够用多久？

A: 对于监控一个地址：
- 每次查询 ~1-10 credits
- 历史数据采集 1000 笔交易：~1000-2000 credits
- 实时监控（每 10 秒）：每天 ~8000-10000 credits
- **大约可以用 1-2 周**

### Q: 如果用完了怎么办？

A: 有几个选择：
1. 等下个月重置（每月 1 号）
2. 升级到付费计划（$10-50/月）
3. 使用公共 RPC（在 .env 中改回默认配置）

### Q: 我的 API Key 安全吗？

A: `.gitignore` 已经配置忽略 `.env` 文件，不会被提交到 git。
但请注意：
- ❌ 不要分享你的 API Key
- ❌ 不要提交 .env 到公共仓库
- ✅ 可以在 Helius Dashboard 随时删除/重新生成 Key

---

## 📞 需要帮助？

如果遇到问题：
1. 检查 API Key 是否复制完整
2. 确认选择的是 Mainnet（不是 Devnet）
3. 查看 Helius Dashboard 的使用状态
4. 运行测试命令检查连接
